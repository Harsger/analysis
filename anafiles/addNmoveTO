filename=$1
specifier=$2
destination=$3
hadd ${filename}_results.root ${filename}_*_${specifier}*
if [ "$?" != "0" ]
then
	echo " ERROR : hadd failed "
	exit 1
fi
mv ${filename}_results.root $destination
if [ "$filename" != "" ]
then
	rm ${filename}_*_${specifier}*
fi
